name: flutter_application_1

description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ">=3.11.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.0
  shared_preferences: ^2.2.0
  fl_chart: ^1.1.1
  url_launcher: ^6.1.11
  image_picker: ^1.2.1
  emoji_picker_flutter: ^4.4.0
  giphy_picker: ^3.0.2
  shadcn_ui: ^0.52.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/Risa_logo.png

